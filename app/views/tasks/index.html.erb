<h1>Tasks page</h1>
<div class="container">
<ul>
<% @tasks.each do |task| %>
<li>
<%= link_to task.title, task_path(task) %>
<%= link_to 'Delete', task_path(task.id), data: {method: :delete, confirm: 'Delete?'} %>
</li>
<% end %>
</ul>
<%= link_to 'Create task', new_task_path %>
</div>
